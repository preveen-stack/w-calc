{
    IBClasses = (
        {
            ACTIONS = {closeIt = id; openIt = id; }; 
            CLASS = AboutBoxController; 
            LANGUAGE = ObjC; 
            OUTLETS = {aboutBox = NSWindow; version = NSTextField; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {closeAlert = id; showAlert = id; }; 
            CLASS = ErrorController; 
            LANGUAGE = ObjC; 
            OUTLETS = {errorDialog = id; errorMessage = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {rowSelected = id; }; 
            CLASS = HistoryList; 
            LANGUAGE = ObjC; 
            OUTLETS = {expressionField = NSTextField; theList = NSTableView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = Interpreter; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {displayPrefs = id; setPrefs = id; showPrefs = id; }; 
            CLASS = PrefsController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                answerField = NSTextField; 
                engineeringNotation = NSButton; 
                expressionField = NSTextField; 
                historyDuplicates = NSButton; 
                historyList = NSTableView; 
                mainController = id; 
                outputFormat = NSMatrix; 
                pickyVariables = NSButton; 
                precisionSlider = NSSlider; 
                printPrefixes = NSButton; 
                thePrefPanel = NSPanel; 
                useRadians = NSButton; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {delVariable = id; newVariable = id; }; 
            CLASS = VariableList; 
            LANGUAGE = ObjC; 
            OUTLETS = {theList = NSTableView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                clear = id; 
                displayPrefs = id; 
                enterData = id; 
                go = id; 
                menuConstant = id; 
                menuFunction = id; 
                setPrecision = id; 
                setPrefs = id; 
                showInspectorDrawer = id; 
                showKeyboardDrawer = id; 
                showPrefs = id; 
                toggleSize = id; 
            }; 
            CLASS = WcalcController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                AnswerField = id; 
                ExpressionField = NSTextField; 
                PrecisionSlider = NSSlider; 
                affectDrawerMenu = NSMenuItem; 
                affectKeyboardMenu = NSMenuItem; 
                capsLockKey = NSButton; 
                engineeringNotation = NSButton; 
                errorController = id; 
                historyDuplicates = NSButton; 
                historyList = NSTableView; 
                keypad = id; 
                mainWindow = NSWindow; 
                outputFormat = NSMatrix; 
                pickyVariables = NSButton; 
                printPrefixes = NSButton; 
                shiftKey1 = NSButton; 
                shiftKey2 = NSButton; 
                theDrawer = NSDrawer; 
                theKeyboard = id; 
                thePrefPanel = NSPanel; 
                updateHistory = NSButton; 
                useRadians = NSButton; 
                variableList = id; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = theDelegate; 
            LANGUAGE = ObjC; 
            OUTLETS = {mainWindow = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}